# pc_beijing: the dock's iGPU output (DP-5) is unused; the monitor hangs off
# the dGPU (DP-3). X names the iGPU outputs <name>-1-<N>, hence "DP-1-5".
# Keeps the sddm greeter (X11) off the dead dock path.
Section "Monitor"
    Identifier "DP-1-5"
    Option "Ignore" "true"
EndSection
