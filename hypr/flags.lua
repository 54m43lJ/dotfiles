-- ~/.config/hypr/flags.lua
-- Device-specific flags — modified by deploy modules (do not edit manually)
-- Each flag defaults to false; deploy modules flip them to true via sed.
return {
    laptop = false,
    pc     = false,
    hidpi  = false,
}
