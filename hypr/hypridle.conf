general {
  lock_cmd = pidof hyprlock || hyprlock
}

listener {
  timeout = 300
  # for testing
  #timeout = 10
  on-timeout = hyprctl dispatch dpms off
  on-resume = hyprctl dispatch dpms on && eww-launcher
}

listener {
  timeout = 330
  # for testing
  #timeout = 11
  on-timeout = loginctl lock-session
}
